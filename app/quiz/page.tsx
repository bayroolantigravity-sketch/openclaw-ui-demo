import QuizFlow from "@/components/QuizFlow";

export default function QuizPage() {
    return (
        <main className="min-h-[100dvh] w-full flex flex-col items-center justify-center p-0 m-0 overflow-hidden relative">
            <QuizFlow />
        </main>
    );
}
